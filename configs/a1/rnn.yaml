# TODO: model is not implemented; this config intentionally fails clearly.
assignment: a1
task: classification
experiment_name: rnn
seed: 42
device: auto
dataset:
  name: fashion_mnist
  root: data
  batch_size: 64
  num_workers: 0
  val_ratio: 0.1
  normalization: true
model:
  name: rnn
training:
  epochs: 10
  learning_rate: 0.001
  weight_decay: 0.0
checkpoint:
  root: checkpoints
logging:
  root: logs
results:
  root: results
